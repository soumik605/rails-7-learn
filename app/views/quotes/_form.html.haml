= form_for quote, html: { class: "quote form" } do |f|
  - if quote.errors.any?
    .error-message
      = quote.errors.full_messages.to_sentence.capitalize
  = f.text_field :name, required: true
  = f.submit class: "btn btn--secondary"
